import { ApprootDirective } from './approot.directive';

describe('ApprootDirective', () => {
  it('should create an instance', () => {
    const directive = new ApprootDirective();
    expect(directive).toBeTruthy();
  });
});
